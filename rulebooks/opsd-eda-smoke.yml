---
- name: OpsDirector EDA smoke test
  hosts: localhost
  sources:
    - ansible.eda.generic:
        payload:
          - dummy: "keep source valid"
  rules:
    - name: Log every event received from OpsDirector
      condition: event.meta is defined or true
      action:
        debug:
          var: event
