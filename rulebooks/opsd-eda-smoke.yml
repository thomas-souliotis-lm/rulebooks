---
- name: OpsDirector EDA smoke test
  hosts: localhost
  sources:
    - ansible.eda.generic:
        payload:
          - dummy_key: "just here to keep the source valid"
  rules:
    - name: Log every event received from OpsDirector
      condition: event.payload is defined
      action:
        debug:
          msg: "Received event from OpsDirector: {{ event.payload }}"
